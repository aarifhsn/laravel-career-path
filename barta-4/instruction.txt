Registration,Login ki breeze diye korbo? (view adjust kore nite parbo)
Naki custom Authentication korbo?

- custom authentication

Orm use korbo na Query Builder?
- যেহেতু আমরা ORM নিয়ে এখনো আলোচনা করিনি, তাই স্বাভাবিক ভাবেই Query Builder ব্যবহার করে অ্যাসাইনমেন্ট টি সম্পন্ন করতে হবে।

Assignment-7 নিয়ে আমার একটা প্রশ্ন ছিল। profiles এর জন্য কি আলাদা একটা database table রাখবো নাকি users table দিয়েই চালায় দিবো?
- users table is ok

"assignment-1" branch er template gula use korte hobe.
- git switch -c assignment-1 --track origin/assignment-1
- do this after clone
